# Техническое задание проекта (актуальная проектная версия)

## Назначение
Система мониторинга — web-панель для управления и контроля Ubuntu-серверов с единым GUI, inventory, status-слоем, alerting, probe-проверками и будущим исполнительным контуром для Ansible.

## Обязательные требования
- основной deploy/update через GitHub + Portainer;
- Docker Compose как обязательный формат поставки;
- контейнеры приложения работают non-root;
- inventory серверов и групп хранится в PostgreSQL;
- состояние проверок хранится отдельно от логов job-ов;
- ping и лёгкие probe-проверки выполняются отдельным слоем, не через Ansible;
- scheduler мониторинга хранит свои настройки в PostgreSQL;
- история прогонов хранится отдельно и доступна из GUI;
- Ansible используется как слой выполнения изменений и административных операций по SSH;
- версия каждого релиза обязательна;
- каждый релиз сопровождается папкой `docs/releases/<version>/`.

## UI-требования
- панель должна иметь левое меню;
- функционал должен быть разнесён по отдельным разделам;
- dashboard не должен смешивать все действия и все таблицы на одном экране;
- inventory должен поддерживать создание, редактирование и удаление серверов и групп;
- связи сервер ↔ группа должны быть обозримыми и управляемыми через GUI;
- интерфейс должен поддерживать светлую и тёмную тему;
- мониторинговые таблицы не должны расползаться: длинные URL, ошибки и технические детали обязаны укладываться в читаемые плашки/компактные блоки без ломания сетки.

## Актуализировано для v0.1.29
- сервер inventory поддерживает поля `host`, `ssh_port`, `ssh_user`, `web_url`, `console_3xui_url`, `subscription_3xui_url` и флаги мониторинга;
- `web_url` используется для проверки HTTP/HTTPS доступности;
- 3x-ui console и subscription проверяются отдельно и не смешиваются с обычным `web_url`;
- должны поддерживаться проверки: ping, SSH, HTTP/HTTPS, 3x-ui, затем SSL, timezone, apt, reboot, журналы;
- alerts по недоступности создаются отдельно по типам `ping_down`, `ssh_down`, `http_down`, `xui_down`;
- scheduler должен уметь запускать due-проверки без ручной кнопки в UI;
- история прогонов должна различать `manual` и `scheduler` source;
- оператор должен видеть последние scheduler-run и историю прогонов без просмотра логов контейнера;
- probe-слой не должен ронять весь API endpoint 500-кой, если проблема возникла в записи результата в БД;
- релиз должен сам выполнять schema-sync старой БД без ручных SQL-правок;
- для non-root контейнера ICMP ping обеспечен через `setcap` на бинарнике и `NET_RAW` в runtime-конфигурации.

## UI hotfix v0.1.34
- экран «Проверки доступности» обязан оставаться читаемым в dark theme;
- статусы probe-контуров отображаются отдельными компактными плашками;
- длинные URL и ошибки не должны разъезжать колонки таблицы.

## Ближайшие функциональные блоки
- `0.1.26` — 3x-ui console/subscription checks и scheduler hotfix;
- `0.1.27` — dashboard/frontend xui settings sync hotfix;
- `0.1.29` — stable image channel for Portainer backend stack;
- `0.1.30` — SSL checks;
- затем timezone checks, apt update/upgrade/dist-upgrade через Ansible, reboot, maintenance windows, UFW/SSH logs и уведомления email/Telegram.
