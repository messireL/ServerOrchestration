# Deploy result v0.1.34

Статус: подготовлено к выкладке.

После redeploy нужно подтвердить:
- таблица доступности читается в dark theme;
- строки не белеют на hover;
- длинные URL и ошибки не расползаются по колонкам;
- плашки статусов/latency видны корректно.
