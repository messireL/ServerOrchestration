# ServerOrchestration v0.1.36

## Что вошло
- синхронизация версии проекта до `0.1.36`;
- новая инструкция `docs/SSH_MIGRATION.md`;
- проброс `summary_json` в слой чтения статусов;
- более информативные подписи контуров HTTP / 3x-ui console / 3x-ui subscription / SSL;
- более аккуратный hover и переносы в таблице статусов.

## Проверка
1. Открыть список серверов и статусную таблицу.
2. Проверить, что у проблемного subscription видна ошибка или HTTP-код, а не только «ok/fail».
3. Проверить SSL-плашку.
4. Убедиться, что версия в UI/API = `0.1.36`.
