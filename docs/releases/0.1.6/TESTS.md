# Tests 0.1.6

Проверить:
- /health
- /version содержит public_base_url
- UI показывает URL панели
- Portainer env позволяет сменить BACKEND_BIND_IP, BACKEND_PORT и APP_PUBLIC_BASE_URL без правки кода
