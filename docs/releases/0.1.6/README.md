# Release 0.1.6

Portainer env hotfix for bind address, port and public base URL.

## What changed
- backend public URL moved to env
- bind IP kept configurable from Portainer
- default bind IP switched to 192.168.5.22
- /version now returns public_base_url
- dashboard shows version, timezone and public URL
