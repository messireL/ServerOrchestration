# Tests v0.1.33

1. HTTP probe сохраняет результат без TypeError.
2. SSL probe сохраняет результат без TypeError по `source`.
3. Connectivity run обрабатывает ping/ssh/http/xui/ssl.
4. В истории проб появляются записи не только по ping.
