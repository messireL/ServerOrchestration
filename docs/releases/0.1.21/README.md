# Release 0.1.21

- исправлен version drift в deploy-артефактах;
- `Dockerfile`, `docker-compose.local.yml` и `docker-compose.portainer.yml` переведены на `0.1.21`;
- сохранён hotfix `0.1.20` по typed `summary_json`/`jsonb` persistence.
