# Tests for 0.1.21

- `/version` возвращает `0.1.21`;
- `/api/probes/ping/diagnostics` доступен;
- Portainer использует image tag `0.1.21`;
- ping probe сохраняет статус без `parameter $7` и без `Internal Server Error`.
