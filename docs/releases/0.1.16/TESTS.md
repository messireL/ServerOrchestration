# Tests 0.1.16

Проверить:
- отображение поля `Web URL` в форме сервера;
- отображение чекбокса `Проверять HTTP/HTTPS`;
- создание сервера через кнопку `Добавить сервер`;
- редактирование сервера с сохранением `web_url` и `has_http_monitoring`;
- работу `/api/servers` и `/api/status/servers` после создания сервера.
