# Проверка релиза v0.1.27

1. Открыть `Главная` — красного JS-баннера больше нет.
2. Открыть `Проверки -> Планировщик мониторинга` — есть поля `3x-ui interval` и `3x-ui timeout`.
3. Сохранить настройки мониторинга и обновить страницу — значения сохраняются.
4. Проверить `/api/probes/history` и убедиться, что `xui` записи от scheduler продолжают появляться.
5. Проверить `/health` и `/version`.
