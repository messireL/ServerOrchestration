# Результат deploy/update v0.1.27

Ожидаемый результат после выкладки:
- dashboard загружается без красного баннера про `Cannot set properties of undefined`;
- в `Планировщик мониторинга` видны поля 3x-ui interval/timeout;
- scheduler продолжает работать и писать историю прогонов `xui`;
- backend `/health` и `/version` отвечают штатно.
