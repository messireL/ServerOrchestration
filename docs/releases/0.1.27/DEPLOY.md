# Deploy / Update v0.1.27

## Основной путь
- обновить существующий Portainer stack из Git-репозитория;
- не создавать новый stack под новый релиз;
- убедиться, что обновились именно frontend static assets backend-контейнера.

## Цель hotfix
После обновления Главная страница должна открываться без JS-ошибки по `setting 'value'`.
