# Bugs 0.1.4

## Известные ограничения
- ping зависит от доступности бинарника `ping` внутри контейнера backend.
- при пустом inventory dashboard работает, но ping probe обрабатывает 0 серверов.
- отдельного GUI для добавления/редактирования серверов в этом релизе ещё нет.
