# ServerOrchestration v0.1.42

- Added HTML subscription page parsing for 3x-ui monitoring.
- Added embedded share-URI detection and embedded base64 decoding inside HTML.
- Accepts subscription headers as valid telemetry even when endpoint returns an HTML landing page.
