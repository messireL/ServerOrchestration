# Известные ограничения v0.1.28

- Переход на `stable` не удаляет автоматически уже накопившиеся старые локальные docker images — для уборки старого мусора всё ещё нужен image prune.
- SSL certificate checks ещё не реализованы — это следующий функциональный блок.
- Для 3x-ui пока проверяется доступность console/subscription URL и HTTP-ответы, но не выполняется полноценная авторизованная бизнес-проверка содержимого панели.
