# Результат deploy/update v0.1.28

Ожидаемый результат после выкладки:
- backend в Portainer использует stable tag;
- контейнер backend остаётся тем же по имени;
- релизная версия читается из `BACKEND_RELEASE_VERSION`;
- приложение работает как раньше без регрессии по dashboard/checks.
