# Tests for 0.1.22

- `/version` возвращает `0.1.22`;
- sidebar показывает версию, TZ, URL панели и оперативную сводку;
- фильтры на экране серверов переключают список без перезагрузки страницы;
- в строке сервера отображаются статусы ping/SSH/HTTP, `last_check` и короткий `last_error`;
- на экране проверок видна summary-панель по ping/SSH/HTTP/alerts;
- запуск ping/SSH/HTTP probe не ломает новый UI-слой.
