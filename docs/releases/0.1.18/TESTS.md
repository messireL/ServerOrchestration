# Tests 0.1.18

- `/version` возвращает `0.1.18`;
- ping probe завершается без ошибки;
- в таблице Dashboard виден ping latency;
- в таблице Проверки виден `ping: N ms`;
- при недоступном хосте статус помечается fail, а latency остаётся пустой/`—`.
