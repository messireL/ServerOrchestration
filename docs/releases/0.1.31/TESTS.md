# Tests v0.1.31

1. Кнопка «Добавить сервер» открывает форму и сохраняет запись без зависания.
2. Редактирование сервера сохраняет `console_3xui_url`, `subscription_3xui_url`, `description`.
3. Ручная проверка 3x-ui идёт по `subscription_3xui_url`.
4. Ручная SSL-проверка идёт по хосту и порту из `subscription_3xui_url`, а не по SSH-порту.
5. В настройках мониторинга сохраняются `ssl_interval_seconds` и `ssl_timeout_seconds`.
6. Scheduler выполняет SSL batch автоматически, а в статусах видно обновление времени последнего запуска.
7. В тёмной теме hover строки сервера остаётся читаемым.
