# Известные ограничения v0.1.26

- В релизе ещё нет SSL certificate checks — это следующий функциональный блок.
- Для 3x-ui сейчас проверяется HTTP/HTTPS доступность console/subscription URL, но не выполняется авторизованный вход или бизнес-проверка содержимого страницы.
- Для стабильных ping-проверок контейнеру по-прежнему нужен `NET_RAW`.
