# Tests 0.1.5

Проверить:
- `/health`
- `/version`
- `/api/summary`
- открытие `/`
- создание группы через web UI
- создание сервера через web UI
- привязку сервера к группе через web UI
- `GET /api/groups`
- `GET /api/servers`
- `POST /api/probes/ping/run`
- обновление статусов и alerts после ping probe
