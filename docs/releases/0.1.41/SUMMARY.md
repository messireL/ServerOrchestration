# ServerOrchestration v0.1.41

- Fixed incorrect 3x-ui enablement check (`has_3xui_monitoring` -> `has_3xui` with URL fallback).
- Moved SSL / HTTP / 3x-ui badge details into hover tooltips so the monitoring table stays readable.
