- `v0.1.50`: backend-парсинг subscription HTML как браузерной страницы (table/text/script fallback) для 3x-ui profile.
- `v0.1.51`: усилен парсинг subscription HTML/JS, убран захват кусков страницы вместо реальных данных подписки, добавлены unit-тесты parser fallback.
- `v0.1.52`: frontend-отображение подписки переведено на русский и очищено от техмусора `HTTP 200` / `html embedded`, если профиль уже успешно разобран.
- `v0.1.53`: синхронизированы version-маркеры в runtime/build, архив очищен от служебного `.pytest_cache`, документация перевыпущена.
- `v0.1.54`: вкладка `Проверки` разгружена: вынесены быстрые действия, добавлены локальные фильтры/поиск/лимиты истории, таблица статусов стала короче и понятнее.

# ServerOrchestration

Web-панель мониторинга и операционного управления Ubuntu/Linux-серверами.

Текущий подготовленный релиз: **v0.1.54**.
Последний подтверждённый пользователем рабочий релиз: **v0.1.38**.

## Что уже есть в v0.1.54
- всё из v0.1.52 по живому backend-парсингу HTML/JS подписок 3x-ui и чистому русскоязычному отображению строки подписки;
- синхронизированы version-маркеры в `VERSION`, backend runtime, frontend и локальном Docker build-контуре;
- из архива убран служебный `.pytest_cache`;
- добавлен `.gitignore` для исключения тестового кэша и Python-мусора;
- обновлена сопутствующая документация релиза.

## Архитектурный стек
- FastAPI backend
- PostgreSQL
- статический frontend внутри backend
- Docker / Docker Compose
- Portainer как основной путь deploy/update
- Ansible как будущий исполнительный слой для операций изменения состояния сервера

## Документация
- ТЗ: `docs/TZ.md`
- Архитектура: `docs/ARCHITECTURE.md`
- Регламент релизов: `docs/RELEASE_POLICY.md`
- Перенос в новый чат: `docs/TRANSFER_TO_NEW_CHAT.md`
- Материалы по релизам: `docs/releases/<version>/`

## Ближайший следующий блок
- **v0.1.53** — clean fix-релиз: version sync + чистый архив после правки subscription profile UI;
- **v0.1.54** — declutter вкладки `Проверки`: статусы/планировщик/история разведены, таблица укорочена, добавлены локальные фильтры;
- дальше: разгрузить экран диагностики и расширять functional/operational-контур мониторинга по серверам.
